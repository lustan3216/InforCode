<%= render 'form' %>

<div class="modal fade" id="title_notice_model" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title pull-left">Notice</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>If change shop title, then Daily Shop Reports will separate to a new report.</p>
        <p>Original daily ad reports still exist after edit ad name.</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" id="title_edit_confirm">Confirm</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" charset="utf-8">
  $('#shop_title').prop('disabled', true)

  $('.shop_title').click(function(){
    if ($('.shop_title input:disabled')[0])
      $('#title_notice_model').modal()
  });

  $('#title_edit_confirm').click(function(){
    $('#shop_title').prop('disabled', false)
    $('#title_notice_model').modal('hide')
  })
</script>